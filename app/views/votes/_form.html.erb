<%= form_for(@vote) do |f| %>
  <% if @vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vote.errors.count, "error") %> prohibited this vote from being saved:</h2>

      <ul>
      <% @vote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %><br>
    <%= f.number_field :number %>
  </div>
  <div class="field">
    <%= f.label :number_of_invalid %><br>
    <%= f.number_field :number_of_invalid %>
  </div>
  <div class="field">
    <%= f.label :constituency_id %><br>
    <%= f.select :constituency_id, @constituencies %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
