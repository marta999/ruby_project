<%= form_for(@voivodeship) do |f| %>
  <% if @voivodeship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@voivodeship.errors.count, "error") %> prohibited this voivodeship from being saved:</h2>

      <ul>
      <% @voivodeship.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :number_of_seats %><br>
    <%= f.number_field :number_of_seats %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
