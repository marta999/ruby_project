<%= form_for(@constituency) do |f| %>
  <% if @constituency.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@constituency.errors.count, "error") %> prohibited this constituency from being saved:</h2>

      <ul>
      <% @constituency.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :number_of_voters %><br>
    <%= f.number_field :number_of_voters %>
  </div>
  <div class="field">
    <%= f.label :voivodeship_id %><br>
    <%= f.select :voivodeship_id, @voivodeships %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
